"""URLs for interactions app."""

from django.urls import path

app_name = 'interactions'

urlpatterns = [
    # Interaction endpoints will be added here
    # POST /api/v1/interactions
    # GET /api/v1/matches
    # GET /api/v1/matches/:matchId/profile
]
