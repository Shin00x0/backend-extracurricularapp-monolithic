"""URLs for work_requests app."""

from django.urls import path

app_name = 'work_requests'

urlpatterns = [
    # Work request endpoints will be added here
    # POST /api/v1/work-requests
    # GET /api/v1/work-requests
    # GET /api/v1/work-requests/:requestId
    # PUT /api/v1/work-requests/:requestId/status
    # POST /api/v1/work-requests/:requestId/accept
    # POST /api/v1/work-requests/:requestId/cancel
    # POST /api/v1/work-requests/:requestId/rate
]
